import utilityTestParser from 'javascript/utils/utility-test-parser';
import { module, test } from 'qunit';

module('Unit | Utility | utility test parser');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = utilityTestParser();
  assert.ok(result);
});
