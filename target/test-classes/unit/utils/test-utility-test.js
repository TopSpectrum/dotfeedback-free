import testUtility from 'javascript/utils/test-utility';
import { module, test } from 'qunit';

module('Unit | Utility | test utility');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = testUtility();
  assert.ok(result);
});
